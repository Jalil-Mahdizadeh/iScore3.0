# Split artifacts

This directory contains immutable row-to-fold assignments and leakage diagnostics. Gate-0/1 headline-style results use union components constructed before fitting; the random-pair view is diagnostic only.

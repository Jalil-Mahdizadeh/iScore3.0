# Gate-0/1 configurations

Every executable Gate-0/1 result must point to a resolved configuration in this directory and an immutable input manifest. Configuration changes produce a new result identity; prior result directories are not overwritten.
